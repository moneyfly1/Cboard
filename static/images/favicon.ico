# Favicon.ico 文件说明

这是一个占位符文件，实际的 favicon.ico 文件应该是一个二进制图标文件。

## 如何生成 favicon.ico：

1. **在线工具**：
   - https://favicon.io/
   - https://www.favicon-generator.org/
   - https://realfavicongenerator.net/

2. **使用 ImageMagick**：
   ```bash
   convert logo.png -resize 32x32 favicon.ico
   ```

3. **使用 GIMP/Photoshop**：
   - 创建 32x32 像素的图像
   - 导出为 .ico 格式

## 推荐尺寸：
- 16x16 (标准)
- 32x32 (高清)
- 48x48 (Windows 任务栏)

## 注意事项：
- 文件大小应小于 100KB
- 支持透明背景
- 在不同背景下都清晰可见
